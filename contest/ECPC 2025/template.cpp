#include<bits/stdc++.h>

using namespace std;

typedef vector<int> vi;
#define f first
#define s second
void fast() {
    ios::sync_with_stdio(0);
    cout.tie(0);
    cin.tie(0);
}

int  main() {
    fast();
    int t;
    cin>>t;
    while (t--) {
        int n;
        cin>>n;
        vi arr(n);
        for (int i=0;i<n;i++){

        }
        cin >> arr[i];
    }
}
