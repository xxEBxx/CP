#include<bits/stdc++.h>
using namespace std;

#define all(x) begin(x), end(x)x

typedef long long ll;
typedef vector<int> vi;

int main(){
    int t;
    cin>>t;
    while (t--) {
        int n;
        cin>>n;
        vi arr(n);
        for (int i=0;i<n;i++)
            cin>>arr[i];
    }

}