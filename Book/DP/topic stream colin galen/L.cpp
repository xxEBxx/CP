#include<bits/stdc++.h>

#define all(v) v.begin(), v.end()
#define f first
#define s second

using namespace std;

typedef long long ll;
typedef pair<int,int> ii;
typedef pair<ll,ll> pl;
typedef vector<int> vi;
typedef vector<vector<int>> vvi;
typedef vector<ll> vl;
typedef vector<vector<ll>> vvl;

const int maxn=(int)1e6+7;
const int mod=(int)1e9+7;

void fast(){
    ios::sync_with_stdio(0);
    cout.tie(0);
    cin.tie(0);
}
int n,m,people[maxn],cost[maxn],tables[maxn];

int main(){fast();
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>people[i]>>cost[i];
    }
    cin>>m;
    for(int i=0;i<m;i++){
        cin>>tables[i];
    }
    map<int,int> change;
    for(int i=0;i<nM)


    return 0;
}