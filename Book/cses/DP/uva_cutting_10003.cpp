#include<bits/stdc++.h>

using namespace std;

int main(){
    int t,n;
    while(true){
        cin>>t;
        if(t==0)break;
        cin>>n;
        vector<int> cuts(n);
        for(int &a:cuts)cin>>a;

    }
}